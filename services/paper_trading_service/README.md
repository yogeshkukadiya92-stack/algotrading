# paper_trading_service

In-memory paper broker adapter for TradePilot India. It implements the shared `BrokerAdapter` contract and never calls a real broker API.

Placeholder service boundary for simulated order fills, positions, and PnL in paper mode.

Paper trading is the prerequisite before any live trading phase.
